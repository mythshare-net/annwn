{
  "name": "annwn",
  "version": "0.2.0",
  "private": true,
  "description": "ANNWN — The Hunt of Arawn: a Mabinogion raycaster",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && node tools/postbuild.mjs",
    "preview": "vite preview",
    "serve": "vite preview --port 4173",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "eslint src tools test",
    "validate": "node tools/validate-levels.mjs"
  },
  "devDependencies": {
    "@eslint/js": "^9.0.0",
    "eslint": "^9.0.0",
    "vite": "^7.0.0",
    "vite-plugin-singlefile": "^2.0.0",
    "vitest": "^3.0.0"
  },
  "dependencies": {
    "rot-js": "^2.2.0"
  }
}
